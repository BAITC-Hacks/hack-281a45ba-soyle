# Default: local deterministic demo. No key or internet required.
AI_MODE=mock

# To enable the single LLM provider, set AI_MODE=openai and add your key locally.
# Server variables only: never add the VITE_ prefix to secrets.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
